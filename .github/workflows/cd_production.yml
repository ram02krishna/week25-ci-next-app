name: Deploy to staging

on:
  push:
    branches:
      - production

jobs:
  redeploy_everything:
    runs-on: ubuntu-latest
    name: Deploying everything to staging

    steps:
      - name: SSH into server
        env:
          STAGING_SSH_KEY: ${{ secrets.STAGING_SSH_KEY }}
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' "$STAGING_SSH_KEY" > ~/.ssh/staging_key
          chmod 600 ~/.ssh/staging_key

          ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o BatchMode=yes -i ~/.ssh/staging_key ubuntu@3.108.236.93 '
            cd ~/week25-ci-next-app &&
            git pull origin production &&
            pnpm install &&
            pnpm build &&
            pm2 restart all
          '
